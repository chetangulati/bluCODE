const React = require('react');
const createClass = require('create-react-class');

var Saved = createClass({
  render: function () {
    return (
      <section id="saved"></section>
    );
  }
});

module.exports = Saved;
